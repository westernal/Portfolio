/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
};

module.exports = {
  images: {
    domains: ["localhost", "res.cloudinary.com"],
  },
};

const nextConfig = {
  experimental: {
    appDir: true,
  },
};
